apiVersion: v1
kind: Service
metadata:
  name: crawler-service
  labels:
    app: crawler
spec:
  type: NodePort
  selector:
    app: crawler
  ports:
    - name: crawler-port
      port: 7878
      protocol: TCP
      targetPort: 7878